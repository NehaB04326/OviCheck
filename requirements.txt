Flask==2.2.2
numpy==1.23.5
torch==2.5.0  # or the latest compatible version
torchvision==0.22.0  # or the latest compatible version
Pillow==9.0.1  # For image processing
setuptools  # Ensure setuptools is included
